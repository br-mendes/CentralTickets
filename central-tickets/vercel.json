{
  "version": 2,
  "buildCommand": "node generate-config.js && echo 'Build completed'",
  "installCommand": "npm install",
  "framework": null,
  "outputDirectory": ".",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
